% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rx2_power_fast.R
\name{fisher_power_fast}
\alias{fisher_power_fast}
\title{Power and sample size for Fisher's exact test (fast variant)}
\usage{
fisher_power_fast(
  n1 = NULL,
  n2 = NULL,
  p1,
  p2,
  alpha = 0.05,
  power = NULL,
  alternative = c("two.sided", "greater", "less"),
  ratio = 1,
  n1_max = 1000L,
  tol = 3.45254e-07
)
}
\arguments{
\item{n1}{Sample size of group 1, or \code{NULL} to solve for it.}

\item{n2}{Sample size of group 2. If \code{NULL}, taken as
\code{ceiling(ratio * n1)} (with \code{n1} the value supplied or
the value being solved for).}

\item{p1}{True event probability in group 1.}

\item{p2}{True event probability in group 2.}

\item{alpha}{Nominal significance level.}

\item{power}{Target power, or \code{NULL} to solve for it.}

\item{alternative}{One of \code{"two.sided"}, \code{"greater"}
(rejects for an excess of events in group 1), or \code{"less"}.}

\item{ratio}{Allocation ratio \code{n2 / n1}, used only when
\code{n2} is not supplied.}

\item{n1_max}{Largest group 1 size to search when solving for
\code{n1}.}

\item{tol}{Tolerance used to decide which tables belong to the
two-sided rejection region (tables whose null probability is
within \code{tol} of the observed table's are included). Matches
the tolerance \code{3.45254e-7} used throughout this package.}
}
\value{
An object of class \code{"power.htest"}, identical in
structure to \code{\link{fisher_power}}'s return value.
}
\description{
A faster variant of \code{\link{fisher_power}} with the same exact
calling convention and numerically identical results, differing
only in cost: the \code{dhyper()} call wasted on one-sided
alternatives is removed, and solving for \code{n1} uses bisection
(which assumes power is non-decreasing in \code{n1}) instead of a
linear scan from 1. Added as a competing pure-R implementation
alongside \code{\link{fisher_power}} in zzfisher's power-function
benchmarking framework.
}
\examples{
fisher_power_fast(n1 = 40, n2 = 40, p1 = 0.3, p2 = 0.1)
fisher_power_fast(p1 = 0.1, p2 = 0.3, power = 0.8)
}
